"use client";
import {useEffect,useState} from 'react';
import {Crown,Layers3,RotateCw,Swords,ZoomIn} from 'lucide-react';
import {Card,getArt} from '@/lib/table';
export function CardArt({card,large=false}:{card:Card;large?:boolean}){const [src,setSrc]=useState<string>();const [failed,setFailed]=useState(false);useEffect(()=>{let live=true;setSrc(undefined);setFailed(false);if(!card.hidden)getArt(card.imageName||card.name,card.tokenImages).then(a=>{if(live)setSrc(card.transformed?a.back||a.front:a.front)});return()=>{live=false}},[card.hidden,card.name,card.imageName,card.transformed,card.tokenImages?.join('|')]);return <div className={`card-art ${large?'large':''} ${card.hidden?'card-back':''}`}>{src&&!failed?<img src={src} alt={card.name} loading="lazy" onError={()=>setFailed(true)}/>:<div className="card-fallback"><Layers3 size={large?44:23}/><strong>{card.hidden?'':card.name}</strong>{!card.hidden&&<small>{card.mana}</small>}</div>}</div>}
export function TableCard({card,onPlay,onInspect,onPreview,disabled=false}:{card:Card;onPlay:(c:Card)=>void;onInspect:(c:Card)=>void;onPreview?:(c:Card|null)=>void;disabled?:boolean}){return <div data-card-id={card.id} className={`table-card ${card.tapped?'is-tapped':''} ${card.selected?'is-selected':''} ${card.actionable||card.selectable?'is-actionable':''} ${card.attacking||card.blocking?'in-combat':''}`}><button className="card-play" disabled={disabled} title={`${card.name} — clicar para jogar ou selecionar`} onClick={()=>onPlay(card)} onMouseEnter={()=>onPreview?.(card)} onMouseLeave={()=>onPreview?.(null)} onFocus={()=>onPreview?.(card)} onBlur={()=>onPreview?.(null)}><CardArt card={card}/><span className="card-name">{card.name}</span>{card.creature&&<span className="card-stats">{card.power}/{card.toughness}</span>}{card.commander&&<Crown className="card-crown" size={14}/>}{card.token&&<span className="token-badge">FICHA</span>}<span className="card-flags">{card.tapped&&<RotateCw size={13}/>} {(card.attacking||card.blocking)&&<Swords size={13}/>} {card.counters&&Object.entries(card.counters).map(([k,v])=><span key={k}>{k} ×{v}</span>)}</span></button><button className="card-zoom" aria-label={'Ampliar '+card.name} onClick={()=>onInspect(card)}><ZoomIn size={13}/></button></div>}
